<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      <%= link_to "request friend", user_request_friend_path(user) %> 
    </li>
  <% end %>
</ul>